import styled from "styled-components";

const StyledTitle = styled.h1`
  font-size: 3em;
  text-align: center;
`;

const Header = () => {
  return (
    <>
      <StyledTitle>WeeDo</StyledTitle>
    </>
  );
};

export default Header;
